plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.4.20'
    id "org.jetbrains.kotlin.kapt" version "1.4.10"
    id 'com.github.johnrengelman.shadow' version '6.1.0'
}

group 'one.oktw'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    jcenter()
    maven { url = "https://jitpack.io" }
    maven {
        name 'velocity'
        url 'https://nexus.velocitypowered.com/repository/maven-public'
    }
}

dependencies {
    kapt 'com.velocitypowered:velocity-api:1.1.1'
    compileOnly 'com.velocitypowered:velocity-api:1.1.1'
    implementation 'io.fabric8:kubernetes-client:4.13.0'
    implementation 'com.fkorotkov:kubernetes-dsl:2.8.1'
    implementation 'io.lettuce:lettuce-core:6.0.1.RELEASE'
    implementation 'one.oktw:galaxy-lib:7f768c8c'
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}

tasks.withType(AbstractArchiveTask) {
    preserveFileTimestamps = false
    reproducibleFileOrder = true
}

shadowJar {
    zip64 true
    archiveClassifier.set('')
}

jar.enabled = false
build.dependsOn(shadowJar)
